// The instance's own annotations: `./annotations` resolves to `annotations.react.ts` or
// `annotations.vue.ts` through the `resolve.extensions` that `main.ts` injects in viteFinal.
export {default} from './annotations'