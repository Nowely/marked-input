---
title: Installation
description: How to install Marked Input
---

You can install the package via npm:

```bash
npm install rc-marked-input
```

or yarn:

```bash
yarn add rc-marked-input
```

or pnpm:

```bash
pnpm add rc-marked-input
```

