import {normalizeMark} from '../../markput/src/utils/functions/normalizeMark'
import {describe, it} from 'vitest'

describe.skip(`Utility: ${normalizeMark.name}`, () => {
	it('should ', () => {
		//normalizeMark
	})
})
