<!-- packages/core/src/features/bridge/README.md -->
# Bridge Feature

Owns the token↔DOM index plus the DOM-side state every other DOM-aware feature reads from. See `DomTokenBridge.ts` for details.

> Body lands in Task 17.
