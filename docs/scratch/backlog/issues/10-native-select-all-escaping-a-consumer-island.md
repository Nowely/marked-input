# Native select-all escaping a consumer island

Status: needs-info

The model correctly stays out, but the native chord moves focus to the container with a collapsed caret at document start. Probe Chromium's behaviour with nested hosts, then choose between scoping it and documenting it.
