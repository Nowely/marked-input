# Forward Delete at a row start merges backward

Status: ready-for-agent

It runs the Backspace branch, so it merges row N into N−1 instead of pulling N+1 up. Reachable since cross-row caret movement became native.
