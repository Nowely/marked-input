# Three caret conventions for a new row

Status: ready-for-agent

Drag-add lands at the row start, Enter on a mark row after the inserted content, select-all + Enter at offset 0. One rule should hold: inside the fresh row's slot.
