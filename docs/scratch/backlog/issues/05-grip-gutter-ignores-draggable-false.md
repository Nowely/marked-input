# The grip gutter ignores `draggable: false`

Status: ready-for-agent

The 24px gutter is reserved only when `draggable` is set, but the grip renders in block mode regardless — it is the menu trigger — so it sits outside the padding.
