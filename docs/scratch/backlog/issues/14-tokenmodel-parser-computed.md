# `TokenModel.#parser` computed

Status: needs-info

`features/tokens/seam/TokenModel.ts:412` — a `Computed<Parser | undefined>`, read at `:372` and re-exposed at `:486`.

Deferred by the 2026-05-23 core audit as real but out of scope. Its cleanup had a separate plan, `docs/superpowers/plans/2026-05-14-tokenmodel-cleanup.md`, which is no longer in the tree and was already stale when the audit ran.

What is missing before this is actionable: what the cleanup was actually going to change, and whether the S1/S2 inversions did it already. Recover the old plan from history first, then decide whether anything remains.
