# Click selects the mark

Status: ready-for-human

Clicking an atomic could select it whole — highlight, Backspace deletes — instead of placing a caret at its nearest edge. New behaviour, not a fix. Decide together with the parked block-selection mode.
