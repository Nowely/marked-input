# Block gap caret

Status: needs-info

Block mode filters the empty bracket tokens, so deleting the only character between two adjacent marks leaves the caret at the document end instead of the deletion site. Three candidate designs; the third changes block's address space, so it is a maintainer call. Produce the comparison table first.
