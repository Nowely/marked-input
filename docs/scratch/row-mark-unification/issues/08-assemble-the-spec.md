# Assemble the spec

Type: grilling
Status: open
Blocked by: 01, 02, 03, 04, 05, 06

## Question

Fold the closed decisions into `docs/scratch/row-mark-unification/spec.md` —
the map's destination: the target facility table (what any node gets, what
stays mark-only, what stays row-only), the migration order with each step
green, the explicit list of observable behavior changes (the map's rule:
every one shown to the maintainer), and amendments to
`docs/scratch/token-born-edit/spec.md` where the phase-4 target surface
moves.

The spec is approved by the maintainer in-session; approval closes the map.
