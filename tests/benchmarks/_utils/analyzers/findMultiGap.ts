export function findMultiGap(value: string, newValue: string) {

	/*if (value === newValue) return

	let firstIndex = value.length
	for (let i = 0; i < value.length; i++) {
		if (value[i] !== newValue[i]) {
			firstIndex = i
			break
		}
	}

	let lastIndex!: number
	for (let i = 1; i <= value.length; i++) {
		if (value.at(-i) !== newValue.at(-i)) {
			lastIndex = value.length - i + 1
			break
		}
	}

	return [firstIndex, lastIndex] as const*/
}