import {Option, OptionProps} from "../components/Option";
import {MarkedInput} from "../components/MarkedInput";

//TODO hook initialize way
const useInput = () => {
    //const A = Option as (props: OptionProps<ButtonProps>) => null
    //return {MarkedInput, A}
}