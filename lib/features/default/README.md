# Default feature

Setting defaults values to option via parsing

Default values of the `options` prop setted only for the `undefined` value.

It's mean options can be `[]`, `[{}]`, `[{data: []}]` etc.